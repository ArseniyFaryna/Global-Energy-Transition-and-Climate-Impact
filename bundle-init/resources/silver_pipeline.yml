resources:
  pipelines:
    energy_silver_pipeline:
      name: ${bundle.target}_energy_silver
      libraries:
        - file:
            path: ../src/silver_dlt/from_bronze_to_silver.py
      catalog: energy_trans_dev
      schema: silver
      development: false
      photon: false
      clusters:
        - label: default
          node_type_id: Standard_F4s_v2 
          num_workers: 1
