resources:
  pipelines:
    energy_bronze_pipeline:
      name: ${bundle.target}_energy_bronze
      libraries:
        - file:
            path: ../src/bronze_dlt/ember_source_to_bronze_dlt.py
      
      configuration:
        spark.ember_api_key: "{{secrets/tokariev-scope/ember-api-key}}"
      
      catalog: energy_trans_dev
      schema: bronze
      clusters:
        - label: default
          node_type_id: Standard_F4
          num_workers: 1
